apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: speech-therapy
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: speech-therapy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: speech-therapy-frontend:latest
          ports:
            - containerPort: 80
